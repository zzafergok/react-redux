Soru : JSX İçerisinde Arrow Function ile Binding

Cevap : JSX içerisinde function'umuzu tanımlarken arrow function kullanarak yazdığımızda da bind etmiş oluruz.

örnek : onClick={() => this.functionAdı()} olarak yazabiliriz.